# RepDetect

## Run locally

1. Install dependencies

```bash
npm install
```

2. Start dev server

```bash
npm run dev
```

3. Open

- http://localhost:3000

## Notes

- Camera access requires HTTPS in production (Vercel provides this automatically).
- All pose detection runs in the browser; no video is uploaded by default.
